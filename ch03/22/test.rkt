;; The basic idea is to transform existing nullary/unary/binary/list into procedures,
;; and then call `value-of`
;; It's really a lot of work to do. And I'm not sure if it's the best way to do so.
;; Also it would be much more easier if we have pattern match.
